import __static__
from __static__ import int64
import time
'Microbenchmark for method call overhead.\n\nThis measures simple method calls that are predictable, do not use varargs or\nkwargs, and do not use tuple unpacking.\n\nbg:\n- annotated all parameters + return types\n- using Timer\n- removed command-line parsing\n'

class Foo(object):

    def foo(self, a, b, c, d) -> None:
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))
        self.bar(a, int64(b), int64(c))

    def bar(self, a, b: int64, c: int64):
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)
        self.baz(int64(a), b)

    def baz(self, a: int64, b: int64) -> None:
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)

    def quux(self, a: int64):
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()

    def qux(self: 'Foo') -> None:
        pass

def test_calls():
    f = Foo()
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    return
if __name__ == '__main__':
    startTime = time.time()
    test_calls()
    endTime = time.time()
    runtime = endTime - startTime
    print(runtime)