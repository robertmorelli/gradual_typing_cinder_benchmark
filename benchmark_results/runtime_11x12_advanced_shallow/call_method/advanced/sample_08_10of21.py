import __static__
from __static__ import int64
import time
'Microbenchmark for method call overhead.\n\nThis measures simple method calls that are predictable, do not use varargs or\nkwargs, and do not use tuple unpacking.\n\nbg:\n- annotated all parameters + return types\n- using Timer\n- removed command-line parsing\n'

class Foo(object):

    def foo(self, a, b, c, d: int64) -> None:
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)
        self.bar(int64(a), b, c)

    def bar(self, a: int64, b, c) -> None:
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))
        self.baz(a, int64(b))

    def baz(self: 'Foo', a: int64, b: int64):
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)

    def quux(self: 'Foo', a: int64) -> None:
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()

    def qux(self) -> None:
        pass

def test_calls():
    f = Foo()
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    return
if __name__ == '__main__':
    startTime = time.time()
    test_calls()
    endTime = time.time()
    runtime = endTime - startTime
    print(runtime)