import __static__
from __static__ import int64
import time
from __static__ import box
'Microbenchmark for method call overhead.\n\nThis measures simple method calls that are predictable, do not use varargs or\nkwargs, and do not use tuple unpacking.\n\nbg:\n- annotated all parameters + return types\n- using Timer\n- removed command-line parsing\n'

class Foo(object):

    def foo(self: 'Foo', a, b, c, d: int64):
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)
        self.bar(int64(a), int64(b), c)

    def bar(self, a: int64, b: int64, c):
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))
        self.baz(box(int64(a)), box(int64(b)))

    def baz(self: 'Foo', a, b):
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)
        self.quux(a)

    def quux(self: 'Foo', a) -> None:
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()
        self.qux()

    def qux(self: 'Foo'):
        pass

def test_calls() -> None:
    f = Foo()
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    f.foo(1, 2, 3, 4)
    return
if __name__ == '__main__':
    startTime = time.time()
    test_calls()
    endTime = time.time()
    runtime = endTime - startTime
    print(runtime)